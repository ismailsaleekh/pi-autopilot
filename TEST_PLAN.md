# Autopilot Test Plan

This plan covers the current Autopilot package capabilities, including worktrees that are sparse by default: `/autopilot`, `/autopilot-inject`, `/autopilot-onboard`, `/autopilot-handoff`, `/autopilot-config`, `/autopilot-claim-gc`, `/autopilot-close`, `/autopilot-abort`, `context_budget`, scheduler config/dispatch, per-unit worktrees, runtime-owned unit mergeback, validation staleness, claim GC, run-owned worktree cleanup/pruning, contracts/templates, Quality vNext contract schemas, forced-output/status, state-store, execution-audit adjudication helpers, work-item lifecycle/closure gates, runtime close/merge/abort, compiled `autopilot-agent-run`, runner CLI/bin behavior, fake-Pi e2e witnesses, parent/onboard/handoff prompts, offline SDK/RPC/package gates, and `pack:dry-run` payload verification. Default validation is deterministic, provider-free, network-free, and isolated from user/global Pi state.

## Coverage matrix

| Feature / README claim | Layer | Automated check |
| --- | --- | --- |
| Pi package manifest exposes the Autopilot extension | Package | `tests/package/package.test.ts` validates `pi.extensions`, keywords, peer deps, files, scripts, docs, bin, and pack payload. |
| Public commands are `/autopilot`, `/autopilot-inject`, `/autopilot-onboard`, `/autopilot-handoff`, `/autopilot-config`, `/autopilot-claim-gc`, `/autopilot-close`, and `/autopilot-abort` | Package / SDK / RPC | Package docs checks, `tests/sdk/activation.test.ts`, `tests/sdk/autopilot-command.test.ts`, and `tests/rpc/command-payloads.test.ts` assert registered command names and command payloads. |
| No public alias outside Autopilot names | SDK / RPC / Package | SDK and RPC tests assert the final command list; package/docs audits assert only Autopilot public names are documented. |
| `context_budget` parent gate | Unit / SDK / RPC | Unit tests cover ok, halt, unknown, rounding, and invalid thresholds; SDK/RPC tests cover activation and invalid `AUTOPILOT_CONTEXT_HALT_PERCENT`. |
| `/autopilot` activates package-owned `context_budget` | SDK / RPC | Real SDK and offline RPC tests invoke `/autopilot` in isolated sessions and verify prompt behavior. |
| `/autopilot-inject` refreshes active-session workstream binding without queueing the parent prompt | SDK / RPC / Package | SDK command tests invoke `/autopilot-inject`, assert `context_budget` activation, assert no parent prompt is queued, and prove a following `/autopilot-handoff` uses the injected workstream instead of warning; offline RPC tests assert command discovery for the public surface. |
| Parent prompt requires `context_budget` before file reads or child launch | Unit / SDK / RPC / Package | Prompt tests and command tests assert startup-gate text; package docs checks keep the README claim mapped to this row. |
| Fixed parent and child model roster | Unit / SDK / Runner / Package | `tests/unit/model-roster.test.ts` proves parent/strategy/validation/adjudication/bughunt use `openai-codex/gpt-5.6-sol` at `xhigh`, implement/fix use `openai-codex/gpt-5.6-terra` at `high`, and extract uses `openai-codex/gpt-5.6-luna` at `high`; parent command tests prove loud missing model/auth failures, while prompt-renderer and runner tests reject deviations before launch. |
| Onboard prompt is read-only and uses `/autopilot-onboard` | Unit / SDK / RPC / Package | Onboard tests assert no launch, mutation, test, provider, or runner execution; package docs include the same limitation. |
| Handoff prompt uses the active workstream and emits a `/autopilot <workstream>` resume block | Unit / SDK / RPC / Package | Handoff tests assert no workstream argument is required, pre-activation calls warn, active-session calls use the recorded workstream, optional comments are preserved, the generated prompt requires a full next-session `/autopilot` block, and `autopilot.handoff.v1` accepts mission/master-plan/decision/state/status/audit refs. |
| Runtime root uses `.pi/autopilot/<workstream>/` | Unit / Package / E2E | Parser/path tests, prompt tests, state-store tests, e2e smoke, README, and package tests assert the authoritative main runtime root even when source-changing units run in per-unit worktrees. |
| Scheduler config persists `parallel_cap` with default 8 and range 1..32 | Unit / SDK / RPC / Package | `tests/unit/phase2-parallelism.test.ts` covers default/read/write/rejection; SDK/RPC tests expose `/autopilot-config`; README/TESTING document the persisted `scheduler-config.json` surface. |
| Deterministic scheduler selects dependency-clear conflict-free units | Unit | `tests/unit/phase2-parallelism.test.ts` covers dependency/lifecycle readiness, lane order, cap enforcement, active-claim conflicts, and skip reasons. |
| Phase 2 per-unit worktrees isolate source-changing units | Unit / Runner / Package | Runner, close, package-bin, and Phase 2 unit tests create/resume deterministic per-unit worktrees, keep metadata outside source status, require implement/fix `cwd` to match the registered unit worktree, and keep artifacts under the main runtime root. |
| Sparse-by-default worktrees and checkout profiles | Unit / Runner / Package | Phase 2 sparse tests prove main/unit worktrees have `core.sparseCheckout=true`, unrelated tracked files stay absent, `_checkout-profile.json` and `_task-info.json` record sparse metadata, `.autopilot/checkout-profile.json` is the documented project profile path, invalid profile parsing fails loudly, and explicit full mode remains opt-in only. |
| Claim-driven materialization and safe READ expansion | Unit / Runner / Package | Runner preflight tests materialize declared `owned_paths`, `read_only_paths`, source `context_refs`, and inspection targets before model spend; sparse tests prove `_materialization-ledger.jsonl`, `_materialized-paths.json`, child-only `autopilot_materialize_context`, auto READ claim expansion, byte/path caps, and WRITE expansion refusal. |
| Sparse disk gate, scalable tracked-tree scan, and loud sparse failures | Unit / Runtime / Package | Disk-gate unit coverage proves projection/refusal arithmetic; a real Git regression fixture produces more than 1 MiB of NUL-delimited `git ls-tree` output and proves the streamed scanner returns every entry without `spawnSync` buffering; worktree creation uses `git worktree add --no-checkout` plus non-cone sparse checkout and refuses sparse/profile/git failures instead of falling back to full checkout. |
| Run-owned worktree cleanup uses scoped proof and git metadata | Unit / Close / Runner / Package | Unit-merge, unit-failure, runner-preflight, close, and abort tests remove only paths derived from the active row (`repo_key + autopilot_id + workstream_run`), clean same-run terminal units before new unit creation, remove merged/reset/aborted units immediately, roll back newly-created unlaunched unit worktrees, remove main worktree and active task dir after archive, prune/reconcile `git worktree list --porcelain` metadata only after run-owned proof, block dirty/running/quarantined residue, and prove a second Autopilot in the same repo key is untouched. |
| Same-parent claims are idempotent only for the same unit attempt | Unit | Phase 2 unit tests prove same parent/unit/attempt claim reuse is allowed while overlapping different units/attempts are rejected. |
| Runtime-owned unit mergeback writes `autopilot.unit_merge.v1` | Unit / Package / Close | Source and rebuilt-dist tests prove mergeback selects the immutable `execution_commit.commit_sha`, clean branch drift blocks before integration mutation or terminal side effects, and the happy path writes merge evidence, releases claims, and retires unit metadata; close runtime uses accepted unit merges as the final diff union proof. |
| Validation staleness and claim GC are evidence-backed | Unit / SDK / RPC / Package | Phase 2 unit tests cover stale validation records and `validationCanCloseSourceWork`; `/autopilot-claim-gc --dry-run|--apply` is exposed through SDK/RPC and releases only proven stale claims. |
| Contracts/templates are schema-backed and package-owned | Unit / Package | `tests/unit/contracts.test.ts` covers unit specs, Quality vNext unit-spec fields, status, events, state, receipts, handoffs, `autopilot.master_plan.v1`, `autopilot.decision.v1`, `autopilot.execution_audit.v1`, semantic rules, evidence metadata, receipt hashes, provider identity, and owned-path enforcement; prompt-renderer tests validate every role template. |
| Perfect-quality doctrine is package-owned | Unit / Package | Parent prompt and prompt-renderer tests assert the perfect-quality doctrine: no band-aids, hacks, silent fallbacks, fake-green tests, fixture tampering, deferred consumers, or self-certifying source-changing work. |
| Quality vNext spec gate rejects weak launches | Unit / Runner / Package | `tests/unit/quality-vnext.test.ts` and runner dry-run tests assert required quality profile, risk level, acceptance criteria, verification plan, closure criteria, mission/master-plan refs for non-strategy specs, declared-command witnesses, required witness expectations, high-risk real-boundary proof/blockers, and validator/adjudicator thinking policy before child launch. |
| Status quality gates reject fake-green success | Unit / Runner | Contract and forced-output tests assert `DONE`/`PASS` reject failed, not-run/blocked, or non-zero command summaries; success statuses must report declared validation commands, validator PASS must cover required witness IDs, unknown witness IDs are rejected, and non-mechanical PASS requires evidence/report refs. |
| Purpose state loads before progress state | Unit / E2E | Purpose-store tests cover `mission.md` sections, `master-plan.json`, append-only `decision-log.jsonl`, decision/master-plan coherence, bounded decision tails, and `readAutopilotResumeSnapshot()` returning purpose before state/events. |
| Execution audits record factual run deltas | Unit / Runner / E2E | Execution-audit tests cover audit path derivation, clean/scope/protected/critical/audit-unavailable classifications, actual vs reported changed paths, committed-path deltas from child-created commits, command coverage gaps, dirty-baseline attribution caveats versus blockers, classification coherence, and runner result/error payload audit path/classification fields. |
| Worktree-scoped git guard blocks outside checkout operations | Unit / SDK / Runner | `tests/unit/git-guard.test.ts`, status-extension tests, prompt tests, and runner tests prove local git is allowed inside the registered Autopilot worktree while `git -C`/`cd`/wrappers/remapping/remote operations outside that worktree, shared branch/tag mutation, and source-tool writes outside allowed worktree/artifact roots are rejected. |
| Scope/protected-path adjudication blocks silent closure | Unit / Package | `tests/unit/adjudication-lifecycle.test.ts` proves outside-owned edits route to scope review until a `scope_exception_ratified` decision amends master-plan ownership, and read-only/untouchable edits block closure until protected-path ownership is explicitly amended after operator approval. |
| Work-item lifecycle separates transport success from closure | Unit / Package | Lifecycle tests prove source-changing transport success moves to validation/audit states rather than closure, each source-changing work item requires its own referenced independent validation PASS before closure, unresolved lifecycle states fail closure, and high-risk work requires final bughunt PASS or accepted blocker. |
| Runtime close/merge/abort is deterministic and local-only | Unit / Package | `tests/unit/close-runtime.test.ts` proves `/autopilot-close` lands validated workstream branches, accepts evidenced in-worktree commit ranges, blocks target path intersections, releases claims, writes close/merge evidence, archives runtime artifacts, removes owned unit/main worktrees plus the active task directory, verifies Git worktree metadata residue is gone, and retires branches without provider/network calls; Phase 2 tests prove accepted unit merge paths and validation-staleness blockers; it also proves `/autopilot-abort` archives abandoned clean workstreams without merging and performs the same owned cleanup. |
| Terminal closure gate rejects unresolved semantic risk | Unit / Package | Closure-gate tests assert running units, unresolved audit/scope/protected exceptions, missing independent validation, and missing high-risk bughunt proof produce blocking reasons instead of `completed` state. |
| Forced-output/status tool is child-only as `autopilot_emit_status` | Unit / SDK / RPC / E2E / Package | Status-extension, forced-output, runner, SDK/RPC, e2e, and package tests assert status+receipt writing and that parent sessions do not expose the child-only surface. |
| Sparse materialization tool is child-only as `autopilot_materialize_context` | Unit / SDK / Package | Status-extension tests assert the helper is registered only in the child runner extension, materializes tracked READ paths through runtime claims, refuses write-scope expansion, and is documented as unavailable to parent sessions. |
| State store | Unit / E2E | `tests/unit/state-store.test.ts` covers atomic state writes, append-only monotonic events, bounded resume snapshots, purpose-before-progress loading, reference validation, and ignoring older runtime locations by default; e2e smoke resumes from `.pi/autopilot/<workstream>/`. |
| `autopilot-agent-run` bin is shipped | Package | Package tests assert manifest bin, compiled `dist/` file existence, help output, pack payload, and installed `node_modules` dry-run behavior without Node type stripping. |
| Runner dry-run validates specs without launching Pi | Unit / Package | Runner unit tests and CLI assertions cover `autopilot-agent-run --dry-run`, prompt snapshots, status context output, and terse/json stdout. |
| Forced-output identity recognizes subscription provider gates while launch uses the fixed roster | Unit / Package | Forced-output tests cover identity mapping for `openai-codex/*`, `anthropic/*`, `opencode-go/*`, `kimi-coding/*`, and `zai/*`; model-roster, prompt-renderer, and runner tests prove launch accepts only the exact role-assigned GPT-5.6 model/thinking pair and rejects all deviations. |
| Runner accepts valid fake child | Unit / E2E | Fake-Pi runner tests require status artifact, receipt artifact, receipt hash, identity, structured tool carrier, and post-audit status reconciliation to join before success. |
| Runner rejects assistant text without structured status | Unit | Runner tests cover missing structured output and invalid structured output failure classes. |
| Runner rejects bad status/receipt and stale output paths | Unit | Runner and forced-output tests cover invalid status, invalid receipt, hash mismatch, mismatched tool carrier, stale status/receipt paths, and invalid specs before model spend. |
| Runner classifies non-success verdicts | Unit | Runner tests cover BLOCKED and NEEDS_FIX status verdicts returning the non-success failure class. |
| Runner classifies Pi errors/timeouts | Unit | Fake-Pi tests cover child exit/error, spawn failures, and wall-timeout handling. |
| CLI/bin supports `--json` and `--pi-executable` | Unit / Package | CLI unit tests cover parsing, json output, executable override, help output, stable exit codes, and compiled-bin execution from installed package layout. |
| Fake-Pi e2e smoke covers status, receipt, event rows, state, and resume | E2E | `tests/e2e/agent-runner-smoke.test.ts` runs a fake Pi child, validates status evidence, writes state/events, and resumes from `.pi/autopilot/<workstream>/`. |
| Offline SDK/RPC/package gates are provider-free | SDK / RPC / Package | SDK harnesses use isolated temp dirs and in-memory managers; RPC uses offline Pi flags; package tests use no network and no provider calls. |
| Published package payload | Package | `npm run build`, `npm run pack:dry-run`, and package tests assert `dist/` runtime files are included and tests/node_modules/artifacts are excluded. |
| README promises map to TEST_PLAN rows | Package | `tests/package/package.test.ts` checks required README claim labels have matching TEST_PLAN rows. |
| Docs and package tests avoid stale legacy-runtime terms | Package | Package docs tests and release wrapper scans fail on stale terms or forbidden legacy runtime identifiers. |
| Remaining limitations are documented | Package | README, TESTING, TEST_PLAN, and PUBLISHING all state that default gates are offline and that live provider-backed child runs require explicit operator approval through subscription Pi channels. |

## Default offline gate

Run from the repository root:

```bash
npm run build
npm run typecheck
npm run test:package
npm run test
npm run pack:dry-run
```

`npm run test` expands to typecheck, type-safety, unit, SDK, RPC, and package suites. E2E fake-runner witnesses live under `tests/e2e/` and are run by the dedicated validation lane when child-runner smoke coverage is required.

Release validation also runs docs audits and forbidden legacy-runtime scans from this standalone repo.

## Manual and opt-in checks

No live provider/model call is part of the default gate. Provider-backed child execution must be an explicit release or operator witness using subscription Pi channels, a non-production workstream, and `autopilot-agent-run` with a reviewed unit spec.

## Known limitations and future coverage

Current automated coverage proves commands, `context_budget`, parent/onboard/handoff prompts, scheduler config/dispatch, sparse-by-default worktrees, checkout-profile snapshots, claim-driven materialization, safe READ expansion, per-unit worktrees, runtime-owned unit mergeback, validation staleness, claim GC, run-owned worktree cleanup/pruning, runtime close/merge/abort, contracts/templates, Quality vNext spec/status gates, durable purpose state helpers, execution-audit generation/validation, scope/protected-path adjudication helpers, work-item lifecycle and terminal closure gates, forced-output/status, state-store helpers, runner dry-run/fake-Pi paths, offline SDK/RPC/package gates, and `pack:dry-run`. Remaining gaps are compiled scheduling UI, PTY/TUI coverage, migration tooling for older runtime folders, default live-provider child execution, network push/PR creation, and hosted PR automation.
