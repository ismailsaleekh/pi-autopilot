# Autopilot Test Plan

This plan covers the current Autopilot package capabilities, including Coordination Fabric contracts, transactional edit leases and change reservations, the transactional coordinator, durable run supervisor, session fencing, child leases, durable mailbox cursors/offline replay, automatic terminal-evidence reconciliation, end-to-end peer claim negotiation, read-only canonical preflight for legacy authority, standalone production surfaces enforcement, executable invariants, failure model, and worktrees that are sparse by default: `/autopilot`, `/autopilot-inject`, `/autopilot-onboard`, `/autopilot-handoff`, `/autopilot-config`, `/autopilot-claim-gc`, `/autopilot-coordination`, `/autopilot-close`, `/autopilot-abort`, `autopilot-coordinator`, `context_budget`, scheduler config/dispatch, per-unit worktrees, runtime-owned unit mergeback, validation staleness, claim GC, run-owned worktree cleanup/exact metadata reconciliation, contracts/templates, Quality vNext contract schemas, forced-output/status, state-store, execution-audit adjudication helpers, work-item lifecycle/closure gates, runtime close/merge/abort, compiled `autopilot-agent-run`, runner CLI/bin behavior, fake-Pi e2e witnesses, parent/onboard/handoff prompts, offline SDK/RPC/package gates, and `pack:dry-run` payload verification. Default validation is deterministic, provider-free, network-free, and isolated from user/global Pi state.

## Coverage matrix

| Feature / README claim | Layer | Automated check |
| --- | --- | --- |
| Pi package manifest exposes the Autopilot extension | Package | `tests/package/package.test.ts` validates `pi.extensions`, keywords, peer deps, files, scripts, docs, bin, and pack payload. |
| Public commands are `/autopilot`, `/autopilot-inject`, `/autopilot-onboard`, `/autopilot-handoff`, `/autopilot-config`, `/autopilot-claim-gc`, `/autopilot-coordination`, `/autopilot-close`, and `/autopilot-abort` | Package / SDK / RPC | Package docs checks, `tests/sdk/activation.test.ts`, `tests/sdk/autopilot-command.test.ts`, and `tests/rpc/command-payloads.test.ts` assert registered command names and command payloads. |
| No public alias outside Autopilot names | SDK / RPC / Package | SDK and RPC tests assert the final command list; package/docs audits assert only Autopilot public names are documented. |
| `context_budget` parent gate | Unit / SDK / RPC | Unit tests cover ok, halt, unknown, rounding, and invalid thresholds; SDK/RPC tests cover activation and invalid `AUTOPILOT_CONTEXT_HALT_PERCENT`. |
| `/autopilot` activates package-owned `context_budget` | SDK / RPC | Real SDK and offline RPC tests invoke `/autopilot` in isolated sessions and verify prompt behavior. |
| `/autopilot-inject` refreshes active-session workstream binding without queueing the parent prompt | SDK / RPC / Package | SDK command tests invoke `/autopilot-inject`, assert `context_budget` activation, assert no parent prompt is queued, and prove a following `/autopilot-handoff` uses the injected workstream instead of warning; offline RPC tests assert command discovery for the public surface. |
| Parent prompt requires `context_budget` before file reads or child launch | Unit / SDK / RPC / Package | Prompt tests and command tests assert startup-gate text; package docs checks keep the README claim mapped to this row. |
| Fixed parent and child model roster | Unit / SDK / Runner / Package | `tests/unit/model-roster.test.ts` proves parent/strategy/validation/adjudication/bughunt use `openai-codex/gpt-5.6-sol` at `xhigh`, implement/fix use `openai-codex/gpt-5.6-terra` at `high`, and extract uses `openai-codex/gpt-5.6-luna` at `high`; parent command tests prove loud missing model/auth failures, while prompt-renderer and runner tests reject deviations before launch. |
| Onboard prompt is read-only and uses `/autopilot-onboard` | Unit / SDK / RPC / Package | Onboard tests assert no launch, mutation, test, provider, or runner execution; package docs include the same limitation. |
| Handoff prompt uses the active workstream and emits a `/autopilot <workstream>` resume block | Unit / SDK / RPC / Package | Handoff tests assert no workstream argument is required, pre-activation calls warn, active-session calls use the recorded workstream, optional comments are preserved, the generated prompt requires a full next-session `/autopilot` block, and `autopilot.handoff.v1` accepts mission/master-plan/decision/state/status/audit refs. |
| Runtime root uses `.pi/autopilot/<workstream>/` | Unit / Package / E2E | Parser/path tests, prompt tests, state-store tests, e2e smoke, README, and package tests assert the authoritative main runtime root even when source-changing units run in per-unit worktrees. |
| Scheduler config persists `parallel_cap` with default 8 and range 1..32 | Unit / SDK / RPC / Package | `tests/unit/phase2-parallelism.test.ts` covers default/read/write/rejection; SDK/RPC tests expose `/autopilot-config`; README/TESTING document the persisted `scheduler-config.json` surface. |
| Deterministic scheduler selects dependency-clear conflict-free units | Unit | `tests/unit/phase2-parallelism.test.ts` covers dependency/lifecycle readiness, lane order, cap enforcement, active-claim conflicts, and skip reasons. |
| Phase 2 per-unit worktrees isolate source-changing units | Unit / Runner / Package | Runner, close, package-bin, and Phase 2 unit tests create/resume deterministic per-unit worktrees, keep metadata outside source status, require implement/fix `cwd` to match the registered unit worktree, and keep artifacts under the main runtime root. |
| Sparse-by-default worktrees and checkout profiles | Unit / Runner / Package | Phase 2 sparse tests prove main/unit worktrees have `core.sparseCheckout=true`, unrelated tracked files stay absent, `_checkout-profile.json` and `_task-info.json` record sparse metadata, `.autopilot/checkout-profile.json` is the documented project profile path, invalid profile parsing fails loudly, and explicit full mode remains opt-in only. |
| Claim-driven materialization and commit-bound READ observations | Unit / Runner / Package | Runner preflight tests materialize declared `owned_paths`, `read_only_paths`, source `context_refs`, and inspection targets before model spend; every READ records and revalidates the exact Git base commit plus blob/tree object, never blocks ordinary WRITE, and enters a wait graph only when an overlapping bounded EXCLUSIVE critical section is active, and sparse tests prove child-only observation expansion, byte/path caps, and WRITE expansion refusal. |
| Sparse disk gate, scalable tracked-tree scan, and loud sparse failures | Unit / Runtime / Package | Disk-gate unit coverage proves projection/refusal arithmetic; a real Git regression fixture produces more than 1 MiB of NUL-delimited `git ls-tree` output and proves the streamed scanner returns every entry without `spawnSync` buffering; worktree creation uses `git worktree add --no-checkout` plus non-cone sparse checkout and refuses sparse/profile/git failures instead of falling back to full checkout. |
| Run-owned worktree cleanup uses scoped proof and git metadata | Unit / Close / Runner / Package | Unit-merge, unit-failure, runner-preflight, close, and abort tests remove only paths derived from the active row (`repo_key + autopilot_id + workstream_run`), clean same-run terminal units before new unit creation, remove merged/reset/aborted units immediately, roll back newly-created unlaunched unit worktrees, remove main worktree and active task dir after archive, reconcile exact `git worktree list --porcelain` metadata only after run-owned proof without a global prune, block dirty/running/quarantined residue, and prove a second Autopilot in the same repo key is untouched. |
| Same-parent claims are idempotent only for the same unit attempt | Unit | Phase 2 unit tests prove same parent/unit/attempt claim reuse is allowed while overlapping different units/attempts are rejected. |
| Runtime-owned unit mergeback writes `autopilot.unit_merge.v1` | Unit / Package / Close | Source and rebuilt-dist tests prove mergeback selects the immutable `execution_commit.commit_sha`, clean branch drift blocks before integration mutation or terminal side effects, and the happy path writes merge evidence, releases claims, and retires unit metadata; close runtime uses accepted unit merges as the final diff union proof. |
| Validation staleness and claim GC are evidence-backed | Unit / SDK / RPC / Package | Phase 2 unit tests cover stale validation records and `validationCanCloseSourceWork`; `/autopilot-claim-gc --dry-run|--apply` is exposed through SDK/RPC. Claim-GC regression tests prove that missing unit-index metadata can release only READ claims backed by an exact completed-state/success-status/execution-audit identity join, dry-run does not release, apply removes only proven keys under the shared claim lock, and running units, mismatched evidence, and WRITE claims remain blocked. |
| Contracts/templates are schema-backed and package-owned | Unit / Package | `tests/unit/contracts.test.ts` covers unit specs, Quality vNext unit-spec fields, status, events, state, receipts, handoffs, `autopilot.master_plan.v1`, `autopilot.decision.v1`, `autopilot.execution_audit.v1`, semantic rules, evidence metadata, receipt hashes, provider identity, and owned-path enforcement; prompt-renderer tests validate every role template. |
| Perfect-quality doctrine is package-owned | Unit / Package | Parent prompt and prompt-renderer tests assert the perfect-quality doctrine: no band-aids, hacks, silent fallbacks, fake-green tests, fixture tampering, deferred consumers, or self-certifying source-changing work. |
| Quality vNext spec gate rejects weak launches | Unit / Runner / Package | `tests/unit/quality-vnext.test.ts` and runner dry-run tests assert required quality profile, risk level, acceptance criteria, verification plan, closure criteria, mission/master-plan refs for non-strategy specs, declared-command witnesses, required witness expectations, high-risk real-boundary proof/blockers, and validator/adjudicator thinking policy before child launch. |
| Status quality gates reject fake-green success | Unit / Runner | Contract and forced-output tests assert `DONE`/`PASS` reject failed, not-run/blocked, or non-zero command summaries; success statuses must report declared validation commands, validator PASS must cover required witness IDs, unknown witness IDs are rejected, and non-mechanical PASS requires evidence/report refs. |
| Purpose state loads before progress state | Unit / E2E | Purpose-store tests cover `mission.md` sections, `master-plan.json`, append-only `decision-log.jsonl`, decision/master-plan coherence, bounded decision tails, and `readAutopilotResumeSnapshot()` returning purpose before state/events. |
| Execution audits record factual run deltas | Unit / Runner / E2E | Execution-audit tests cover audit path derivation, clean/scope/protected/critical/audit-unavailable classifications, actual vs reported changed paths, committed-path deltas from child-created commits, command coverage gaps, dirty-baseline attribution caveats versus blockers, classification coherence, and runner result/error payload audit path/classification fields. |
| Worktree-scoped git guard blocks outside checkout operations | Unit / SDK / Runner | `tests/unit/git-guard.test.ts`, status-extension tests, prompt tests, and runner tests prove local git is allowed inside the registered Autopilot worktree while `git -C`/`cd`/wrappers/remapping/remote operations outside that worktree, shared branch/tag mutation, and source-tool writes outside allowed worktree/artifact roots are rejected. |
| Scope/protected-path adjudication blocks silent closure | Unit / Package | `tests/unit/adjudication-lifecycle.test.ts` proves outside-owned edits route to scope review until a `scope_exception_ratified` decision amends master-plan ownership, and read-only/untouchable edits block closure until protected-path ownership is explicitly amended after operator approval. |
| Work-item lifecycle separates transport success from closure | Unit / Package | Lifecycle tests prove source-changing transport success moves to validation/audit states rather than closure, each source-changing work item requires its own referenced independent validation PASS before closure, unresolved lifecycle states fail closure, and high-risk work requires final bughunt PASS or accepted blocker. |
| Runtime close/merge/abort is deterministic and local-only | Unit / Package | `tests/unit/close-runtime.test.ts` proves `/autopilot-close` lands validated workstream branches, accepts evidenced in-worktree commit ranges, blocks target path intersections, releases claims, writes close/merge evidence, archives runtime artifacts, removes owned unit/main worktrees plus the active task directory, verifies Git worktree metadata residue is gone, and retires branches without provider/network calls; Phase 2 tests prove accepted unit merge paths and validation-staleness blockers; it also proves `/autopilot-abort` archives abandoned clean workstreams without merging and performs the same owned cleanup. |
| Terminal closure gate rejects unresolved semantic risk | Unit / Package | Closure-gate tests assert running units, unresolved audit/scope/protected exceptions, missing independent validation, and missing high-risk bughunt proof produce blocking reasons instead of `completed` state. |
| Forced-output/status tool is child-only as `autopilot_emit_status` | Unit / SDK / RPC / E2E / Package | Status-extension, forced-output, runner, SDK/RPC, e2e, and package tests assert status+receipt writing and that parent sessions do not expose the child-only surface. |
| Sparse materialization tool is child-only as `autopilot_materialize_context` | Unit / SDK / Package | Status-extension tests assert the helper is registered only in the child runner extension, materializes tracked READ paths through runtime claims, refuses write-scope expansion, and is documented as unavailable to parent sessions. |
| State store | Unit / E2E | `tests/unit/state-store.test.ts` covers atomic state writes, append-only monotonic events, bounded resume snapshots, purpose-before-progress loading, reference validation, and ignoring older runtime locations by default; e2e smoke resumes from `.pi/autopilot/<workstream>/`. |
| `autopilot-agent-run` bin is shipped | Package | Package tests assert manifest bin, compiled `dist/` file existence, help output, pack payload, coordinator-fenced child preflight, and installed `node_modules` dry-run/live fake behavior without Node type stripping. |
| Legacy migration + recovery CLI | Unit / Crash / Package | Sanitized real-corpus generations prove closed historical normalization, pre-capture evidence preservation without inferred release, stale metadata reconciliation, 1 MiB JSONL bounding, runtime pause import, 1,014-claim paginated retention below the IPC frame ceiling, immutable release evidence, recovery-CLI non-retirement plus global-lock-owned all-repository drain/retirement, cross-repository rollback fencing, recovery/retirement serialization, >256-run catalog pagination, exact replay pending counts, and exact dead handoff-session drain. Packed installation must expose both npm `.bin` commands and the recovery surface. |
| Transactional coordinator | Unit / Multiprocess / Scale / SDK / Package | `tests/unit/coordinator-runtime.test.ts` proves WAL store migration, verified backup, SQLite integrity plus full production invariants, deterministic export, strict framing, idempotent single effects, optimistic versions, child/session durability, and no heartbeat-based authority release. The shipped `autopilot-coordinator replay` producer stages bounded canonical current-protocol mutation JSONL. Startup streams validated records into SQLite staging and commits the complete effects plus schema-12 completion atomically; filesystem receipts are symlink-safe derived projections and stale receipts cannot suppress replay after DB restore. `tests/crash/semantic-replay-resume.test.ts` hard-kills subprocesses at stage/batch, DB-completion, receipt, and cleanup boundaries. |
| Encoded-byte-bounded coordinator responses | Unit / Crash / Multiprocess / Scale | `tests/unit/coordinator-pagination.test.ts` proves the old 2,127,035-byte activation `attach-session`, exact per-field contributions, 1,806-byte compact replay, 24,576 lossless ordered reconciliation details, generalized reconstruction of 1,024 mutation-result entities, compact 4,000-message immutable mailbox paging across restart/live acknowledgement, parser/frame checks before commit, forged/cross-scope/drift cursor refusal, and status/doctor availability. The >256-run corpus additionally proves a concurrent new run cannot drift into or invalidate a file-backed catalog snapshot. `tests/crash/coordinator-response-commit-resume.test.ts` SIGKILLs after commit but before response and proves one exact replay/effect/generation. Every actual page is capped by encoded bytes, 1,024 entities, and a 524,288-byte single-entity limit. |
| Coordinator version boundary and historical continuity | Multiprocess / SDK / Unit | Compatibility tests pin `1.1.5-cf47` as current and admit cf46/cf45/cf44/cf43 only through the closed protocol-1.6/schema-12 matrix after exact predecessor certification. They retain cf42 as schema-11 migration input, reject schema-10/schema-9 live brokers before mutation, preserve historical locks/intents only as exact read-only identities, and prove same-socket re-attestation never sends a mutation to an unknown replacement. |
| Locked package/coordinator upgrade | Unit / Multiprocess | `tests/unit/coordinator-upgrade-contracts.test.ts` locks the executable 1.2/schema-6/build identity, process-creation identity, old-format handoff fence, generation-separated v2 lifecycle identity, authenticated status, and exact prior-protocol replay boundary—without an upgrade-control action absent from 1.2. `tests/multiprocess/coordinator-upgrade-process.test.ts` `git archive`s and builds `aa3e377`, runs its actual packaged bin on Linux/macOS/Windows, and proves bounded checkpoint/critical-section refusal; verified preflight and byte-exact final backups; an all-schema-6-table mutation-deny barrier committed while the exact predecessor remains alive; already-open heartbeat, acquisition, general mutation, and prepared-statement denial; fail-closed birth-identity-checked cross-platform process signaling without suspension only after that commit; private-copy schema-12 publication/start/reconnect; hard death immediately after barrier commit but before predecessor retirement or intent publication; actual-1.2 direct-restart refusal, pre-intent reconstruction, exact-old retirement on resume, and racing-client serialization; a real stale-1.2 attack after the freshness window; and byte-exact rollback that loudly requires manual old-binary recovery. |
| Durable run supervisor | Unit / SDK / Runner | Supervisor/store tests prove one durable run record, session attach/detach, process-bound child leases, mailbox delivery/ack, running-child heartbeat/completion after parent replacement, and ownership retention across replacement; SDK activation attaches before prompt dispatch and runner tests register child authority before fake model launch and prove the parent session context/capability is absent from the child Pi environment. |
| Lifecycle response-loss and worktree identity recovery | Unit / Multiprocess / Crash / Migration | Independent startup clients attest one exact lifecycle winner even when an election-loser child exits; genuine child death without an endpoint fails quickly. A real cf45 binary with an attached generation-9 session is certified through socket loss and concurrent cf47 recovery; endpoint recovery wins, while persistent outage permits only lock/token/instance/process-birth-identity-fenced graceful retirement and elected replacement with sessions, children, operations, and leases unchanged. Owned sagas inject death/response loss at action, report, verification, and commit boundaries, probe before retry, execute once, preserve WRITE/EXCLUSIVE authority, reject stale sessions, and report the exact durable phase plus bounded provenance-labelled cause/reconciling evidence through IPC and the operator-visible attachment diagnostic. A pre-spend remove response-loss fixture commits the durable effect, verifies no child launched for that exact attempt, resumes index/branch/attempt projection cleanup idempotently, preserves an unrelated strategy child, and keeps heartbeat authority live while source-changing dispatch remains blocked. Migration/runtime share deterministic worktree IDs. Doctor exposes schema-12 exact duplicates before repair; the production registration/observation path transactionally retires only an exact redundant projection with an audit event, preserves rows/operations/events, remains idempotent after restart, and fails authority/state/version/history drift loudly. |
| Session fencing | Unit / SDK / Multiprocess | Repeated handoff/replacement tests prove old generations reject mutation, per-session lease capabilities prevent PID-reuse impersonation, handoff retains child/run state, and coordinator restart retains the latest generation. |
| Canonical repository-grounded authority | Unit / Runner / Scheduler / Materialization | `tests/unit/authority.test.ts`, runner tests, and Phase 2 scheduler/materialization tests prove one deterministic persisted `autopilot.authority.v1` artifact binds observations to exact commits/objects, classifies edit intentions/future-owned paths/directory breadth, rejects prose and untracked context/witness claims, caps broad scope, and is consumed by scheduler, runner, acquisition, materialization, and audit without independently inferred sets. |
| End-to-end peer claim negotiation | Unit / Multiprocess / Scheduler | `tests/unit/claim-negotiation.test.ts` proves bounded EXCLUSIVE atomic acquisition, owner-request dedupe/delivery/acknowledgement, typed defer with owner-bound conditions, idempotent retry/handoff replay and stale reordering, atomic release notification, deterministic bounded offer/expiry on timer and mutation paths, live grant acknowledgement, request/group cancellation, attempt supersession, cross-repository identity isolation, and starvation-safe multiple-waiter ordering. `tests/multiprocess/coordinator-process.test.ts` runs owner and requester through independent Node clients; scheduler tests retain exact `waiting-for-peer-release` request refs. |
| Bounded EXCLUSIVE operation policy | Unit / Model / Multiprocess / Crash / Migration | `tests/unit/exclusive-policy.test.ts` rejects free-form kinds, mismatched sections, broad scopes, missing WRITE layers, multiple operations, invalid duration/release metadata, role/spec invention, and unsafe partial authority. Claim tests prove exact checkpoint exit after coordinator restart retains WRITE, age never releases authority, doctor diagnostics, schema-10 legacy conversion, and idempotent replay. Model/deadlock/multiprocess suites prove only EXCLUSIVE creates wait edges, no unsafe victim is selected, and release/notification/grant remain transactional. |
| Offline mailbox replay and automatic reconciliation | Unit / SDK / Multiprocess / Runtime | `tests/unit/offline-reconciliation.test.ts` proves owner offline, requester offline, both offline, session replacement, coordinator restart, stable at-least-once replay, and carrier-bound `autopilot.child_terminal_acceptance.v1` completion. Startup repair requires either that acceptance chain or one exact historical parent `agent_completed` fact; synthetic receipts/failure evidence and heartbeat age cannot release WRITE authority. |
| Historical pending failure-evidence regeneration | Unit / Runtime / Crash-replay | BUG-178 coverage proves an unaccepted exact historical reset/abort pending intent is never replayed as release authority: the runtime first publishes an immutable digest-bound supersession receipt, preserves the original evidence bytes, removes only that exact intent from the active queue, and regenerates strict current evidence at a distinct digest-derived path from verified Git/worktree facts. Replayed supersession is idempotent; quarantine/preserve, malformed, forged, cross-run, changed, oversized, or symlinked evidence fails loudly while leases remain retained. |
| Terminal non-success and failed edit authority | Runner / Unit / Crash | Runner tests prove valid `NEEDS_FIX`/`BLOCKED` commits parent acceptance before surfacing `status-non-success`, releases observations, resets only mechanically clean zero-source-change attempts, preserves dirty tracked/untracked/ignored work through exact worktree quarantine plus immutable capture ref before WRITE/EXCLUSIVE release, and never routes accepted `DONE` source work into failed-authority quarantine before mergeback. Ambiguous completion acknowledgement is reconciled idempotently and never followed by a contradictory recovery transition. |
| `/autopilot-coordination` status/doctor is read-only observability | SDK / RPC / Package | SDK command tests query status/doctor without queueing an LLM turn; command discovery and docs cover the public surface; store tests prove doctor reports integrity and expired-heartbeat recovery without releasing WRITE authority. |
| `autopilot-coordinator` bin is shipped | Package / Multiprocess | Manifest/pack tests assert the compiled bin, help, packed generic-repository status query, database/schema files, and source-independent startup. |
| Runner dry-run validates specs without launching Pi | Unit / Package | Runner unit tests and CLI assertions cover `autopilot-agent-run --dry-run`, prompt snapshots, status context output, and terse/json stdout. |
| Forced-output identity recognizes subscription provider gates while launch uses the fixed roster | Unit / Package | Forced-output tests cover identity mapping for `openai-codex/*`, `anthropic/*`, `opencode-go/*`, `kimi-coding/*`, and `zai/*`; model-roster, prompt-renderer, and runner tests prove launch accepts only the exact role-assigned GPT-5.6 model/thinking pair and rejects all deviations. |
| Runner accepts valid fake child | Unit / E2E | Fake-Pi runner tests require status artifact, receipt artifact, receipt hash, identity, structured tool carrier, and post-audit status reconciliation to join before success. |
| Runner rejects assistant text without structured status | Unit | Runner tests cover missing structured output and invalid structured output failure classes. |
| Runner rejects bad status/receipt and stale output paths | Unit | Runner and forced-output tests cover invalid status, invalid receipt, hash mismatch, mismatched tool carrier, stale status/receipt paths, and invalid specs before model spend. |
| Runner classifies non-success verdicts | Unit | Runner tests cover BLOCKED and NEEDS_FIX status verdicts returning the non-success failure class. |
| Runner classifies Pi errors/timeouts | Unit | Fake-Pi tests cover child exit/error, spawn failures, and wall-timeout handling. |
| CLI/bin supports `--json` and `--pi-executable` | Unit / Package | CLI unit tests cover parsing, json output, executable override, help output, stable exit codes, and compiled-bin execution from installed package layout. |
| Fake-Pi e2e smoke covers status, receipt, event rows, state, and resume | E2E | `tests/e2e/agent-runner-smoke.test.ts` runs a fake Pi child, validates status evidence, writes state/events, and resumes from `.pi/autopilot/<workstream>/`. |
| Offline SDK/RPC/package gates are provider-free | SDK / RPC / Package | SDK harnesses use isolated temp dirs and in-memory managers; RPC uses offline Pi flags; package tests use no network and no provider calls. |
| Published package payload | Package | `npm run build`, `npm run pack:dry-run`, `payload:check`, and package tests assert compiled coordinator/migration runtime plus deliberate security/SBOM artifacts are included while tests, node_modules, and transient artifacts are excluded. |
| README promises map to TEST_PLAN rows | Package | `tests/package/package.test.ts` checks required README claim labels have matching TEST_PLAN rows. |
| Docs and package tests avoid stale legacy-runtime terms | Package | Package docs tests and release wrapper scans fail on stale terms or forbidden legacy runtime identifiers. |
| Coordination Fabric contracts and protocol lock | Unit / Package | `tests/unit/coordination-contracts.test.ts` parses every Phase 27 entity from `unknown`, rejects unknown fields/actions, checks closed JSON schemas, validates query/mutation envelope identity, and proves the stable failure taxonomy. |
| Coordination invariants and transition model | Unit / Model | Coordination contract tests cover ownership, non-blocking observations and speculative WRITE intentions, strict bounded EXCLUSIVE leases, deferred promises, release notifications, operations, reservations, terminal intents, and escalation; `tests/model/coordination-model.test.ts` runs deterministic generated grant/fencing sequences and proves no partial acquisition, stale-session mutation, or duplicate effect. |
| Edit lease / change reservation separation | Unit / Multiprocess / Runner / Close | `tests/unit/integration-conflicts.test.ts`, `tests/unit/edit-lease-reservations.test.ts`, Phase 2 tests, and `tests/multiprocess/coordinator-process.test.ts` prove overlapping WRITE intentions launch in independent worktrees without requests; exact Git-backed merge evidence creates actual-path reservations; merge-tree, hunk, delete/modify, protected-surface, and JSON semantic-key facts classify overlap; disjoint same-file hunks auto-integrate in deterministic predecessor order and mark prior validation stale; major conflicts create repair-routing evidence; close still requires current integration plus independent validation and terminal reservation landing. The real fake-Pi runner suite uses coordinator-authoritative v2 workstreams and complete groups; scheduler, runner dispatch, and close consume obligation/reservation proof. |
| Transactional deadlock and starvation engine | Unit / Model / Multiprocess | `tests/unit/deadlock-escalation.test.ts` generates 2–32-node rings and DAGs, rejects post-initial WRITE expansion, proves durable running checkpoints/preemption, dirty-preflight refusal, critical-section/preemptibility/progress/live-cycle grant-order policy, multi-cycle SCC fixed-point resolution, crash/restart replay, exact scheduling-decision aging, and the integrated eight-bypass threshold. `tests/multiprocess/coordinator-process.test.ts` proves disjoint EXCLUSIVE operations create no synthetic wait graph, while persistent contested-EXCLUSIVE clients exercise real release/retry/handoff ordering; generated cyclic graphs remain a direct unit/model proof because immutable all-or-nothing initial acquisition prevents ordinary process clients from manufacturing hold-and-wait. |
| Contradiction-only escalation arbiter | Unit / Coordinator | `tests/unit/deadlock-escalation.test.ts` rejects every operational escalation class, registers schema-valid authoritative Git-HEAD artifacts through their source runs, requires a coordinator assignment to a non-participating adjudication-role attempt, delivers the bounded immutable source bundle only through that run's authenticated assignment claim, completes adjudication in a separate authenticated child process, retains bounded immutable evidence bytes, rejects unaccepted/oversized evidence, deduplicates submission, and creates exactly one `planning-contradiction` packet. |
| Legacy coordination preflight has real consumers | Unit / SDK | `tests/unit/coordination-legacy-preflight.test.ts` proves canonical parsing, input hashing, bounded diagnostics, durable old-session claim retention warnings, malformed/incompatible authority refusal, and authority-byte immutability; activation SDK coverage and claim-GC tests prove `/autopilot` and claim-GC dry-run invoke it in production paths. |
| Standalone package boundary | Package | `tests/package/coordination-isolation.test.ts` scans package-owned runtime/docs, rejects closed runtime and product assumptions, refuses symlink traversal, and runs in the default package gate. |
| Verified legacy migration and one-way cutover | Unit / Crash / Chaos / Package | `tests/unit/coordination-migration.test.ts` proves mutation-free live-schema-6-WAL dry-run via a bounded disposable db/WAL/SHM copy outside the source tree, exact source-byte/SHM stability, exact schema-6/7/8/9/10/11/12 checksum lineage, schema-10 observations, schema-11 EXCLUSIVE conversion, and schema-12 receipt/delivery cutover, strict bounded parsing, global freeze/drain, snapshot/hash, verified backup, mixed Phase 34 state merge, transactional import, immutable run resources, old-session rebinding, claim-path/Git-diff-bound terminal release, typed ambiguous recovery, filesystem/Git verification, repeatable rollback generations, one-way marker, manifested read-only archive, writer fences, and post-archive activation without fallback. `tests/crash/migration-resume.test.ts` interrupts every durable/commit-before-journal and migration-lock candidate/reclaim/release transition with real hard death; `tests/crash/migration-recovery-evidence-resume.test.ts` hard-kills synced-temp and published evidence writes and proves a partial final cannot strand recovery; `tests/chaos/migration-chaos.test.ts` proves stale `.reclaim` resume, byte/Git drift restoration, duplicate-key rejection, and final/parent symlink refusal. |
| Legacy READ retirement and parent claim-response consumer | Unit / SDK / Migration | `tests/unit/legacy-read-release.test.ts` proves pre-import terminal classification and schema-9 unbound READ retirement with the original lease/recovery payload in immutable audit, no retroactively fabricated source identity, WRITE non-release, and immediate overlapping-WRITE grant; `tests/unit/claim-response-tool.test.ts`, `claim-negotiation.test.ts`, and SDK activation tests prove the active parent-only typed tool, exact current request lookup, cross-run refusal, release-now, and typed defer. |
| Coordination release scale | Multiprocess / Scale | `tests/multiprocess/coordinator-process.test.ts` keeps real 5/10/32-process client cohorts alive concurrently while seeded schedules interleave acquire/defer/retry/handoff/cancel/replacement/release and coordinator crash. Retry, defer, and handoff mutations are separately driven, and production `doctor`/`checkCoordinationInvariants` runs immediately after every commit. `tests/scale/coordination-scale.test.ts` production-replays exactly **100,000** real current-protocol mutation events, including exactly **10,000** contested claim requests and exactly **32** logical client identities, with no test-only event schema and no raw SQL. It parses deterministic export counts, sums DB+WAL+SHM, and times all status forms plus real mailbox drains. Exact limits map one-to-one to assertions: wall time **<60s**, peak RSS **<512 MiB**, SQLite footprint **<256 MiB**, indexed queries **<1s**, event rows **=100,000**, claim-request rows **=10,000**, full invariants/integrity `ok`, and repeated export SHA-256 values equal. |
| Cross-platform packed release, security, and SBOM | Package / CI | `.github/workflows/release-certification.yml` is configured for minimum/current Node on Linux/macOS/Windows and invokes `test:packed-migration`; that focused lane installs the generated tarball into a generic consumer, scans the installed boundary, and rehearses apply→verify→rollback plus apply→verify→cutover through the packed coordinator. On Windows the migration test inspects `attrib` for `R`, attempts replacement and append mutations in child processes, requires both to fail, and rechecks exact archive bytes. Security scripts retain audit/SBOM/payload evidence. |
| Remaining limitations are documented | Package | README, TESTING, TEST_PLAN, and PUBLISHING map the Phase 27–35 implementation and QA surfaces without treating CF-9 scaffolding as release completion; default gates are offline, and live provider-backed child runs require explicit operator approval through subscription Pi channels. |

## Default offline gate

Run from the repository root:

```bash
npm run build
npm run typecheck
npm run test:package
npm run test
npm run pack:dry-run
```

`npm run test` expands to typecheck, type-safety, offline security scan, unit, no-spend fake-Pi E2E, Coordination Fabric model/property, multiprocess, crash, chaos, 100k-event scale, SDK, RPC, package, and payload suites. The E2E lane uses a real temporary coordinator/session/child lease but no provider or network call.

Release validation also runs docs audits and forbidden legacy-runtime scans from this standalone repo.

## Manual and opt-in checks

No live provider/model call is part of the default gate. Provider-backed child execution must be an explicit release or operator witness using subscription Pi channels, a non-production workstream, and `autopilot-agent-run` with a reviewed unit spec.

## Known limitations and future coverage

Phase 32 crash/recovery coverage includes every durable orchestration boundary plus prepared, partial-action, and post-action boundaries for main/unit create, pre-commit metadata gating/finalization, sparse materialization, execution commit, mergeback, quarantine capture, reset, archive refs, branch retirement, worktree removal, and exact metadata verification. The suite exercises branch-only/worktree-only sparse create, stale-lock reclaimers, ENOSPC/EACCES/EEXIST classification and retry, unresolved LFS refusal, interrupted merge-conflict compensation, final-target response loss across coordinator restart, exact stale-metadata repair without foreign global pruning, dirty destructive refusal, branch/ref movement, path substitution, foreign-session mutation, and handoff fencing.

Current automated scaffolding exercises Coordination Fabric Phases 27–35: contracts, protocol envelopes, invariants, transition/failure models, transactional WAL store, migrations/backups/integrity/export, authenticated framed IPC, single-writer election, idempotency, kill/restart recovery, durable run supervisors, session/child leases, handoff fencing, durable mailbox cursors/offline replay, startup reconciliation, automatic child/merge/reset/quarantine/close/abort release-condition watchers, all-or-nothing acquisition groups, owner-request dedupe/delivery/acknowledgement, release-now/deferred promises, atomic release notifications, deterministic bounded offers, live requester grant acknowledgement, cancellation, attempt supersession, multiple-waiter fairness, two-process negotiation, scheduler waiting refs, child authority preflight, legacy preflight, standalone isolation, commands, `context_budget`, parent/onboard/handoff prompts, scheduler config/dispatch, sparse-by-default worktrees, checkout-profile snapshots, claim-driven materialization, commit-bound observation expansion, per-unit worktrees, runtime-owned unit mergeback, validation staleness, claim GC, run-owned worktree cleanup/exact metadata reconciliation, runtime close/merge/abort, contracts/templates, Quality vNext spec/status gates, durable purpose state helpers, execution-audit generation/validation, scope/protected-path adjudication helpers, work-item lifecycle and terminal closure gates, forced-output/status, state-store helpers, runner dry-run/fake-Pi paths, offline SDK/RPC/package gates, and `pack:dry-run`. Phase 32 additionally covers owner-scoped durable worktree/Git operation intents, exact authority, probe/action/verification replay, dirty capture, branch/ref fencing, startup recovery messages, and foreign-run cleanup refusal. Phase 33 covers coordinator-backed runner acquisition, exact merge-to-reservation conversion, terminal lease release, deterministic foreign reservation obligations, integration/revalidation proof, scheduler reasons, reservation-backed close proof, and terminal-intent dispatch fencing. Phase 34 covers persisted wait-for edges, generated cycle detection, deterministic safe-victim selection, no-safe-victim deferral, starvation aging and threshold priority, multiprocess no-synthetic-deadlock plus contested-EXCLUSIVE traces, strict authoritative-document/adjudication proof, exact packet dedupe, and mechanical rejection of every operational escalation class. Phase 35 scaffolding covers resumable migration, rollback-before-cutover, hash drift, verified backup/restore, transactional import, one-way cutover, archived-writer fences, post-archive runtime consumers, transition crash boundaries, seeded 5/10/32-process traces, the 100k/10k scale corpus, cross-platform workflow configuration, package payload, security audit, and SBOM. These checks emit candidate evidence; CF-9 completion remains a release-lead decision after the hosted matrix and independent bughunt. The separate opaque `tool_call_id` regression is certified through one shared receipt/terminal runtime-and-schema contract: arbitrary pipes round-trip exactly, Unicode code-point boundaries agree, NUL is rejected, and no normalization occurs. Remaining out-of-scope surfaces are compiled scheduling UI, PTY/TUI coverage, default live-provider child execution, network push/PR creation, and hosted PR automation.
