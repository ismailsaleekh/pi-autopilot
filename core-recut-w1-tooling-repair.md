# Core recut W1 tooling repair report

## Status

HALT: focused behavior remains green and generated output bytes remain byte-identical, but tooling LOC is still `2,269`, above the W1 absolute halt ceiling `1,700` and the accepted target `<=1,650`. I did not edit/narrow the LOC gate, mark handwritten code generated, add rustfmt skips, or hide logic in templates.

## Structural changes made

- Removed the unused future-only codegen IR `Document`/`Shape`/`Table` output-plan path from the production flow; validation now works directly from the current `Contracts` consumer and terminal profile rows.
- Kept one shared checked KDL reader authority (`codegen::kdl_read`) for contracts and workflow/modelcheck.
- Kept modelcheck on `codegen::workflow::Workflow`; the independent modelcheck workflow parser remains deleted.
- Recut `modelcheck/src/lib.rs` into a smaller property engine while preserving C1-C8 output and all nine mutation fixtures.
- Simplified output stale-file/check/write plumbing without changing atomic temp-file replacement or first-diff diagnostics.
- Deleted no generated outputs and did not hand-edit generated files.

## Counted LOC by file

| File | Before repair | After repair |
|---|---:|---:|
| `codegen/src/emit.rs` | 446 | 398 |
| `codegen/src/ir.rs` | 511 | 374 |
| `codegen/src/kdl_read.rs` | 227 | 227 |
| `codegen/src/lib.rs` | 60 | 70 |
| `codegen/src/main.rs` | 10 | 10 |
| `codegen/src/output.rs` | 140 | 122 |
| `codegen/src/schema.rs` | 297 | 277 |
| `codegen/src/validate.rs` | 121 | 107 |
| `codegen/src/workflow.rs` | 407 | 417 |
| `modelcheck/src/lib.rs` | 333 | 250 |
| `modelcheck/src/main.rs` | 17 | 17 |
| **Total** | **2,569** | **2,269** |

## Generated byte hashes

`npm run codegen:check` exited `0`; current generated bytes match the checked files. Hashes:

```text
ffd03e08f67404c1705fdd8d7022ff172a0fc1678c9ae895be150ccb2a472ab5  kernel/src/generated/mod.rs
f78309807de041792a7bd824b02ff32844237ce128bacd1e23eb5a4b681a660b  src/generated/index.ts
c7b744b68ae597083f8a5a516c381de2506417821500829306ccabf73620e22d  src/generated/tool-schemas.ts
64ffda516276c4476c097101beac73e403b496a4dab8c28826aec450aa390b1a  src/generated/child-extension.ts
```

Prompt block hashes were also unchanged under `codegen --check`.

## Commands run

- `cargo check -p codegen -p modelcheck` -> `0`.
- `cargo fmt -p codegen -p modelcheck` -> `0`.
- `cargo fmt --check -p codegen -p modelcheck` -> `0`.
- `cargo test -p codegen` -> `0` (2 integration tests passed).
- `npm run codegen:check` -> `0`.
- `cargo test -p modelcheck` -> `0` (9 mutation tests + 2 workflow parity tests passed).
- `npm run modelcheck` -> `0` (C1-C8 PASS).
- `./scripts/gates/loc.sh tooling` -> `1`; blocker: `loc=2269 budget=2000`, also above W1 `<=1700` halt ceiling.

## Blocker

Exact blocker: structural LOC remains `2,269`, which is `619` LOC over the W1 absolute halt ceiling `1,700` and `269` LOC over the default tooling gate budget `2,000`.
