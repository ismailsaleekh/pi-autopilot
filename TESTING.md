# Testing Autopilot

Autopilot tests are deterministic by default. They do not call live providers, networks, real models, or user/global Pi state while validating Coordination Fabric contracts, the transactional coordinator, durable run supervisors, session/child fencing, durable mailbox cursors/offline replay, carrier-bound terminal acceptance and automatic reconciliation, commit-bound non-blocking observations, strict WRITE/EXCLUSIVE authority, executable invariants, failure/transition models, legacy coordination preflight, standalone package isolation, and the fixed GPT-5.6 parent/child role roster, loud parent-selection failures, roster-gated unit specs, and `/autopilot`, `/autopilot-inject`, `/autopilot-onboard`, `/autopilot-handoff`, `/autopilot-config`, `/autopilot-claim-gc`, `/autopilot-coordination`, `/autopilot-close`, `/autopilot-abort`, `autopilot-coordinator`, `context_budget`, scheduler config/dispatch, sparse-by-default checkout profiles, disk-gate projection, claim-driven materialization, child-only `autopilot_materialize_context`, per-unit worktrees, runtime-owned unit mergeback, validation staleness, run-owned worktree cleanup/exact metadata reconciliation, contracts/templates, Quality vNext contract schemas, perfect-quality prompt doctrine, worktree-scoped git guards, scope/protected-path adjudication, work-item lifecycle/closure gates, runtime close/merge/abort, forced-output/status, state-store helpers, `autopilot-agent-run`, runner CLI/bin behavior, fake-Pi paths, offline SDK/RPC/package gates, and package payloads.

## Default offline gate

Run from the repository root:

```bash
npm run build
npm run typecheck
npm run test:package
npm run test:packed-migration
npm run test:upgrade
npm run test
npm run pack:dry-run
```

`npm run test` expands to:

```bash
npm run typecheck
npm run test:type-safety
npm run security:scan
npm run test:unit
npm run test:e2e
npm run test:model
npm run test:multiprocess
npm run test:crash
npm run test:chaos
npm run test:scale
npm run test:sdk
npm run test:rpc
npm run test:package
npm run payload:check
```

`security:scan` is deliberately third in that exact chain; `sbom` and registry-backed `security:audit` are not nested in `npm run test`. Release validation runs `npm run sbom` and `npm run security:audit` separately, plus docs audits and forbidden legacy-runtime scans from this standalone repo.

## What the suites cover

- `build` emits runtime JavaScript into `dist/` so the published `autopilot-agent-run` and `autopilot-coordinator` bins never type-strip TypeScript from `node_modules`.
- `typecheck` and `test:type-safety` enforce strict TypeScript and ban type escapes across source, extension, and tests.
- Unit, E2E, and SDK files run with Node test concurrency `1` because they deliberately replace process-scoped environment authority; serialization prevents one isolated state root from ever becoming another test file's coordinator authority.
- `test:unit` separately certifies the shared opaque `tool_call_id` contract across receipt runtime, terminal-acceptance runtime, and both JSON Schemas: leading/trailing/doubled pipes round-trip unchanged, 200 astral Unicode code points pass, 201 fail, and NUL fails everywhere.
- `test:unit` includes cf47 lifecycle regressions: exact active worktree duplicates and authority drift are visible to doctor; a schema-12 migration-ID/current-ID fixture is transactionally retired through the production prepare-operation consumer; canonical rows plus all operations/events remain; replay is a no-op; observation lookup remains exactly-one; and response-loss saga boundaries report the durable operation ID and exact prepare/probe/action/report/verification phase while retaining authority. Unsafe preflight proof and server-side reconciling evidence survive the typed IPC/client/extension path under closed entry/text/total bounds, explicit truncation, and credential redaction; no action runs, the operation remains reconciling, and the attached session remains fenced exactly as before. A cf46 response-loss fixture with a committed pre-spend remove and missing legacy ledger projection forward-recovers only the deterministic unlaunched unit metadata, preserves an unrelated live strategy child, and proves owned recovery blocks source-changing dispatch without stopping heartbeat lease renewal.
- `test:unit` includes the BUG-176 encoded-byte corpus: a 128-run catalog is 148,316 bytes while the immediately following cf42 `attach-session` is 2,127,035 bytes, with all six reconciliation collection contributions accounted exactly. It migrates 24,576 exact cf42 details into a 1,806-byte replay plus 24 pages, reconstructs a 1,024-lease mutation from generalized result details, proves empty reconciliation creates no redundant receipt row, drains 4,000 immutable mailbox messages across restart/new-arrival/replay/live acknowledgement without duplicating payload JSON, freezes a >256-run file-backed catalog across a concurrent arrival, rejects forged skip cursors and oversized single entities, proves every response parser/frame check runs before commit, and exercises byte-paged status/doctor. It also covers sanitized real-corpus migration generations (pre-checkout v1, checkout-era v1, coordination-only v2, current v2, stale unit creation snapshots, missing branch/worktree recovery, supplemental adjudication evidence, >64 KiB bounded JSONL, 1,014-claim paginated bulk retention, and exact dead handoff-session drain), recovery-only CLI fencing/idempotency/mailbox cursors, and strict Coordination Fabric entity/protocol parsers, closed schemas, ownership/fencing/lease/request/message/operation/escalation invariants, failure taxonomy and matrix, bounded legacy authority preflight, durable old-session claim retention warnings, malformed/incompatible authority refusal, transactional SQLite migration/backup/integrity/export, framed IPC validation, idempotent mutation, optimistic versions, session/child leases with private capabilities, running-child continuity across parent handoff, PID-reuse impersonation rejection, handoff fencing, durable mailbox delivery/acknowledgement cursors, owner/requester/both-offline replay, startup repair, audit-preserving supersession of unaccepted historical pending reset intents plus distinct current-evidence regeneration, `NEEDS_FIX`/`BLOCKED` terminal acceptance, child/merge/reset/quarantine/close/abort condition watchers, canonical repository-grounded authority derivation/persistence with tracked/prose/future-owned/broad-scope gates, exact observation/speculative-edit/exclusive separation and edit-lease-to-change-reservation conversion, merge-tree/hunk/protected/semantic integration classification, automatic disjoint-hunk reservation integration, repair routing, crash-resumable per-validation staleness propagation without filename collisions, successful-DONE quarantine exclusion, integration/revalidation obligations, fenced terminal intents, session/child heartbeat-expiry classification without age-based release, `context_budget`, command parsing, fixed parent/child model roster resolution and mismatch rejection, parent prompt, onboard prompt, handoff prompt rendering, scheduler config/dispatch, sparse checkout profile parsing, a real-Git tracked-tree scan whose NUL-delimited output exceeds Node's historical 1 MiB synchronous child-output buffer, disk-gate arithmetic, sparse main/unit worktree creation, claim-driven materialization, commit/blob/tree-bound non-blocking READ expansion, per-unit worktrees, same-parent edit-intent semantics, runtime-owned unit mergeback, validation staleness, claim GC including completed-state/status/receipt/audit fallback for metadata-missing READ claims and running/WRITE fail-closed witnesses, legacy READ terminal classification and schema-9 unbound-READ audit retirement without fabricated provenance, with forged-receipt/WRITE/cross-run negatives, the authenticated parent-only `autopilot_respond_claim_request` release/defer consumer, run-owned worktree cleanup/exact metadata reconciliation before unit creation, after merge/reset/abort, on runner preflight rollback, and on close/abort, Autopilot contracts/templates, Quality vNext unit-spec fields, deterministic spec-quality gates, status fake-green rejection, master-plan/decision/audit schemas, purpose-store helpers, worktree-scoped git guard decisions, execution-audit generation/classification/coherence including child-created commits, scope/protected-path adjudication, work-item lifecycle and terminal closure gates, perfect-quality prompt doctrine, forced-output/status identity and receipts, state-store read/write/resume behavior, and `autopilot-agent-run` dry-run/fake-Pi/error handling.
- `test:e2e` runs the no-spend fake-Pi runner/status/receipt/state/resume witness with a real temporary coordinator, session authority, child lease, Git worktrees, and proof that parent authority is scrubbed before child spawn.
- `test:model` executes deterministic generated coordination transitions covering session fencing, idempotent attachment replay, all-or-nothing grant, the full non-blocking READ/speculative WRITE/bounded EXCLUSIVE compatibility matrix, and atomic release-plus-notification.
- `test:multiprocess` additionally races independent startup clients and requires one lifecycle PID/instance, exact winner attestation by every client, fast failure for genuine child death without an endpoint, and same-socket rejection of unknown/incompatible or replaced endpoints before any operation. A real cf45 binary with an attached generation-9 session loses its socket and is replaced exactly once under concurrent cf47 clients without losing the session. A synthetic socketless cf44 owner proves endpoint recovery wins without signaling, persistent outage triggers only exact process-birth-identity retirement, stale current/predecessor fences are elected away, and the cf47 replacement preserves and heartbeats the original active session, child, and WRITE lease. It also proves applied external effects survive coordinator response loss and restart without duplicate action, verification, commit, or lease release.
- `test:multiprocess` keeps 5/10/32 independent client processes alive concurrently while a seeded topological scheduler changes operation categories as well as actor order. Persistent clients execute EXCLUSIVE acquire, typed defer, exact duplicate/stale retry, handoff fencing, cancellation plus attempt replacement, release, acknowledgement, and coordinator SIGKILL/restart replay interleavings on one shared critical path; a separate real-process witness proves overlapping WRITE intentions both grant without negotiation. Compound retry/defer/handoff helpers are split so every coordinator mutation or idempotent replay is separately driven; after every commit, the production `doctor` runs full `checkCoordinationInvariants` and the test also checks exact authority/fairness projections. A seeded wave writes operations to distinct long-lived child processes before awaiting any response, so independent sockets race at the coordinator rather than merely traversing a shuffled array. Version-boundary coverage materializes the exact `v1.0.1` package from Git, treats its protocol-1.3/schema-9 coordinator as an incompatible migration predecessor, and proves no 1.1.1 mutation crosses that live endpoint; same-socket handshake-before-operation, unknown replacement refusal, unavailable-live-broker preservation, elected current-lock recovery, and historical-intent fencing are explicit negatives. The schema-changing upgrade suite separately `git archive`s `aa3e377`, builds its actual package, runs its packaged coordinator bin, and proves authenticated status drain, fail-closed birth-identity-checked signaling, a schema-6 byte-exact final backup plus all-table mutation-deny barrier committed while the predecessor remains alive, incompatible-barrier/private-copy schema-12 publication/start/reconnect, critical-section refusal, already-open heartbeat/acquire/general mutation and pre-prepared-statement denial, a hard kill immediately after barrier commit but before predecessor retirement or intent publication, actual-1.2 direct-restart refusal, digest-verified intent reconstruction, exact-old retirement on resume, racing-client serialization, byte-exact rollback with honest manual-restart guidance, and a real stale-1.2 attack after the target has exceeded the predecessor freshness window.
- `test:crash` includes a real SIGKILL after `attach-session` event/idempotency commit but before socket encoding, then proves exact compact replay, one generation, one event/result, and complete receipt recovery. It also injects failure after every durable and commit-before-journal migration boundary, then uses real subprocess SIGKILL at database import commit, cutover-marker commit, and projection promotion; every case resumes idempotently through archive completion.
- `test:chaos` proves post-import byte or Git drift restores the verified database boundary and requires a fresh snapshot, rejects duplicate JSON keys, and refuses final-file or parent-directory symlink escapes.
- `test:scale` stages exactly 100,000 real current-protocol idempotent mutations through the shipped operator/startup replay path—no test-only event schema and no raw SQL. Coordinator startup streams every canonical record into a transactional SQLite stage, verifies exact schema/count/digest and pinned-file identity, then applies all effects plus the schema-12 completion row in one transaction. A database restore cannot be suppressed by a stale derived receipt. The corpus includes exactly 10,000 contested requests and 32 logical client identities across 256 repositories. Under a 256 MiB V8 old-space ceiling, the test enforces `<60s` for production staging plus the complete transactional startup replay, then separately parses deterministic exports to assert exact entity counts, sums DB+WAL+SHM, and times status for all 256 repositories plus actual idempotent mailbox-drain queries. It also enforces `<512 MiB` RSS, `<256 MiB` SQLite footprint, and `<1s` indexed queries.
- `test:sdk` loads the extension through the real Pi SDK with isolated temp `cwd`/`agentDir`, in-memory managers, no built-in tools by default, and offline environment variables. It asserts `/autopilot`, `/autopilot-inject`, `/autopilot-onboard`, `/autopilot-handoff`, `/autopilot-config`, `/autopilot-claim-gc`, `/autopilot-coordination`, `/autopilot-close`, `/autopilot-abort`, durable supervisor attachment, shutdown/handoff fencing, coordination status/doctor, active-workstream handoff behavior, lazy parent-session exposure of `context_budget` plus authenticated `autopilot_respond_claim_request`, and no claim-response tool before run attachment.
- `test:rpc` starts an offline `pi --mode rpc` process with isolated HOME/session directories and validates command discovery including `/autopilot-inject`, `/autopilot-config`, `/autopilot-claim-gc`, `/autopilot-coordination`, plus `/autopilot`, `/autopilot-onboard`, and `/autopilot-handoff` command payloads.
- `test:package` validates standalone package isolation, manifest/docs/runtime files, public surfaces, README-to-TEST_PLAN mapping, stale-docs audits, runner/coordinator help output, packed `node_modules` behavior, installed-tarball boundary scanning, and packed generic-repository apply→verify→rollback plus apply→verify→cutover rehearsals. `npm run test:packed-migration` exposes that rehearsal as a focused cross-platform CI lane.
- `pack:dry-run` and `payload:check` confirm the published payload contains runtime files (`bin/`, `dist/`, `extensions/`, `src/`, `templates/`, docs, license), compiled migrations, and deliberate `artifacts/security/` evidence while excluding tests, transient artifacts, and dependency directories. Payload validation recomputes `package-lock.json` SHA-256 and fails if the offline scan is stale.
- `security:scan` is deterministic/offline and writes its audited dependency-source/install-script report; `prepack` runs it mandatorily before SBOM generation. `security:audit` is a release-CI registry audit and is intentionally not part of offline default QA; `sbom` deterministically generates CycloneDX 1.5 evidence.
- `tests/e2e/` contains no-spend fake-Pi witnesses for runner/status/receipt/state/resume flows and runs in the default `test:e2e` lane; it is not a live-provider test.

## Environment

Recommended environment for automated runs:

```text
PI_OFFLINE=1
PI_SKIP_VERSION_CHECK=1
PI_TELEMETRY=0
CI=1
```

The SDK and RPC suites create their own temporary Pi state and should not read or mutate user-global Pi configuration. BUG-176 certification is synthetic/sanitized and isolated: it does not read, stop, migrate, or modify any live production coordinator, database, lock, socket, WAL/SHM, claims, mailbox, worktree, or artifact.

## Artifacts

Transient package artifacts stay ignored under `artifacts/`. `artifacts/security/offline-security-scan.json` and `artifacts/security/cyclonedx-sbom.json` are deliberate, deterministic release evidence and are included in the package payload.

## Live-provider policy

The default gate does not prove live provider behavior. Provider-backed child runs are opt-in only, require explicit operator approval, must use subscription Pi channels, and must launch through `autopilot-agent-run` with a reviewed Autopilot unit spec. Runner model gates allow only `openai-codex/*`, `anthropic/*`, `opencode-go/*`, `kimi-coding/*`, and `zai/*`; do not add OpenRouter or metered frontier API routes to tests or docs.

## Known limitations

Automatic predecessor retirement remains birth-identity-checked PID signaling rather than an OS-handle-bound atomic kill; macOS requires `/usr/bin/python3` to bridge to libproc and fails closed when either dependency/ABI identity is unavailable.

Current tests exercise cf47 pre-spend rollback projection recovery and heartbeat dispatch blocking plus the cf46 endpoint-first startup election, phase-accurate owned-saga recovery, deterministic worktree identity, transactional/auditable semantic projection deduplication, and Coordination Fabric Phases 27–35 contracts, protocol envelopes, invariants, transition/failure models, transactional WAL storage, migrations/backups/integrity/export, authenticated framed IPC, idempotency, single-writer/restart behavior, durable supervisors, session/child leases, handoff fencing, durable mailbox cursors/offline replay, startup reconciliation, automatic terminal-evidence release-condition watching, atomic acquisition groups, request dedupe/delivery/acknowledgement, typed defer/release, atomic release notification, bounded grant offers and fake-clock expiry, live grant acknowledgement, cancellation, attempt supersession, deterministic multiple-waiter ordering, immutable initial acquisition authority, typed observation-only expansion, persisted EXCLUSIVE-backed wait-for edges, generated and overlapping-SCC fixed-point cycle handling, durable running checkpoint/preemption, dirty-preflight refusal, no-safe-victim restart replay, exact scheduling-decision aging, integrated eight-bypass starvation priority, real-process speculative-WRITE grants plus contested-EXCLUSIVE negotiation and no-synthetic-deadlock proof, source-run Git-HEAD artifact registration, coordinator-assigned separate-process adjudication with terminal child evidence, bounded immutable evidence, contradiction-only escalation arbitration, owner-scoped worktree/Git operation intents and recovery, dirty-work quarantine preservation, exact branch/common-dir/path authority, foreign-run isolation, scheduler peer-wait and reservation-ordering/integration refs, coordinator-backed runner acquisition, exact reservation-backed close proof, production legacy preflight consumers, standalone isolation, shipped commands, `context_budget`, parent/onboard/handoff prompts, scheduler config/dispatch, sparse checkout profile parsing, disk-gate arithmetic, sparse main/unit worktrees, claim-driven materialization, commit-bound observation expansion, per-unit worktrees, runtime-owned unit mergeback, validation staleness, claim GC, run-owned worktree cleanup/exact metadata reconciliation, runtime close/merge/abort, contracts/templates, Quality vNext contract schemas, deterministic spec/status gates, durable purpose state, worktree-scoped git guards, execution-audit generation/classification/coherence, child-created commit evidence, scope/protected-path adjudication, work-item lifecycle and terminal closure gates, perfect-quality prompt doctrine, forced-output/status, state-store helpers, runner CLI/bin, fake-Pi and e2e witnesses, offline SDK/RPC/package gates, durable migration/cutover with rollback and writer fences, transition crash injection, source-drift/symlink chaos, 5/10/32 clients, 100k-event/10k-request scale, cross-platform packed release CI, security audit/SBOM, and `pack:dry-run`. These suites produce release-candidate evidence; CF-9 completion and publish certification remain explicit release-lead decisions after matrix CI and independent bughunt review. Remaining out-of-scope surfaces are compiled scheduling UI, PTY/TUI behavior, default automated live-provider child execution, network push/PR creation, and hosted PR automation.
