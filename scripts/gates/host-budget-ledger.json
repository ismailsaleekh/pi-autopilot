[
  {
    "from": 1200,
    "to": 1207,
    "actualBefore": 1199,
    "actualAfter": 1207,
    "sourceDelta": 8,
    "date": "2026-08-02",
    "justification": "Host budget exception: ceiling 1200 -> 1207. Canonically formatted Host LOC changes from 1199 to 1207 (+8 source LOC). The machine-readable status transport must use pi.appendEntry, which exists only on ExtensionAPI, not ExtensionContext. applyCoreEffect receives only ctx and HostEffectServices, so the adapter must capture this capability at the extension entrypoint and thread it through the existing services seam, exactly as the sibling operatorMessage sink already is. Delta: +3 shared status-channel constant/type/payload builder, +1 StatusEntrySink type, +2 service/options fields, +1 services pass-through, +1 entrypoint closure. The done-case edit is net-zero. The shared builder is imported by BOTH production and the LIVE controller fixture, so a fixture can no longer hand-roll a status frame the product never emits. The delta adds no branch, parsing, classification, mapping, default, retry, fallback, or status policy: Core remains the sole semantic authority. 1207 is the minimum sufficient ceiling, with no discretionary headroom. Any later increase requires a new before/after measurement, itemized boundary justification, and architecture-owner approval."
  },
  {
    "from": 1207,
    "to": 1123,
    "actualBefore": 1207,
    "actualAfter": 1123,
    "sourceDelta": -84,
    "date": "2026-08-04",
    "justification": "DOWNWARD re-scope: ceiling 1207 -> 1123, the exact measured actual, with zero banked headroom. WHAT MOVED: src/child-extension-runtime.ts moved to child-runtime/child-extension-runtime.ts. The move itself was byte-identical (sha256 87343410bfff4205fe711178e05a0f0a90630b5853f48fb0f6d4d2cef8578cf2, unchanged across the move). Two path-reference lines were then updated in place and nothing else: the type-only import of the generated tool schemas ('./generated/tool-schemas.ts' -> '../src/generated/tool-schemas.ts') and selfDigest's own-file URL ('../child-extension-runtime.ts' -> '../../child-runtime/child-extension-runtime.ts'). Post-edit sha256 is 014a61b364e3d0e1ffe5e239e62847d363ebf5099843845802644ec3d74db7d0. No logic, control flow, or enforcement line was edited; LOC is unchanged at 536, so the arithmetic below is unaffected. WHY IT IS NOT HOST: that module is not the operator-session Pi extension. The Host is extension.ts, activation.ts, commands.ts, transport.ts, host-runtime.ts, effects.ts. The relocated module is the runtime loaded inside a SPAWNED CHILD Pi agent (launched --no-extensions -e <addon>); it decides nothing, and mechanically enforces an allowlist Core already decided and cryptographically pinned, using opaque byte equality with zero parsing. Counting it against the Host budget made every added confinement check cost Host headroom, i.e. it priced security fixes against a gate scoped at the wrong component. GOVERNANCE: it is not ungoverned. It is now governed by the new sibling gate scripts/gates/child-runtime-thinness.sh plus scripts/gates/child-runtime-budget-ledger.json, which mirror host-thinness.sh mechanics exactly (same non-comment/non-blank counting, same '@generated by codegen' exclusion, same hard ceiling, same ledger key set and from/to continuity check, same BUDGET == ledger.to assertion), seeded at its exact measured actual with zero headroom, with a banned vocabulary that is the Host decision list verbatim plus this component's own inference/shell-semantics/fuzzy-matching/silent-fallback/escape-hatch risks. It is wired into gate:release next to gate:host-thinness. It also remains inside readable-source.mjs scanning (SCAN_ROOTS gained 'child-runtime'), inside typecheck (tsconfig.json, tsconfig.build.json, host/tsconfig.json), inside the no-foreign-tool-ownership invariant (roots gained 'child-runtime'), and inside the shipped payload allowlist (package.json files[] gained child-runtime/, check-payload.mjs gained an exact single-path rule). NO HOST DECISION LOGIC WAS REMOVED, HIDDEN, OR DELETED: this change adds, deletes, and rewrites no Host semantics whatsoever; the entire Host source set is untouched by this change apart from the file's absence from the directory. HONEST DISCLOSURE: the module REMAINS in the parent Host's import graph (extensions/autopilot.ts -> src/generated/child-extension.ts -> child-runtime/child-extension-runtime.ts) and is still loaded into the operator's Pi session. This change does NOT make it unloaded by the Host, and does NOT thin the Host. What the parent actually reaches is registerSubmitTools and CHILD_RECEIPT_ENTRY; runAutopilotChild and all delivery-policy enforcement are reachable only in a spawned child. NET DELTA (-84) HAS EXACTLY TWO COMPONENTS: (1) -91, removal of the runtime's baseline 91 lines from Host scope, the same 91 lines that were already inside the 1207 measurement; (2) +7, retained legitimate growth in src/pi-coding-agent-shim.d.ts, namely the BashOperations/EditOperations/WriteOperations type aliases and the defineTool-adjacent createBashTool/createEditTool/createWriteTool/createLocalBashOperations declarations. Those 7 ambient declaration lines exist solely to type the child runtime's tool overrides, are referenced by no Host source file, and stay in Host scope because the shim is a single ambient module declaration under src/ that cannot be split without duplicating the module augmentation. -91 + 7 = -84. 1123 is the exact measured actual; the ~84 lines of slack are surrendered, not banked. Any later increase requires a new before/after measurement, itemized boundary justification, and architecture-owner approval."
  }
]
