[
  {
    "from": 536,
    "to": 536,
    "actualBefore": 536,
    "actualAfter": 536,
    "sourceDelta": 0,
    "date": "2026-08-04",
    "justification": "Seed entry for the child delivery runtime budget, at the EXACT measured actual with ZERO headroom. Scope: child-runtime/**/*.ts, non-comment/non-blank lines, generated files excluded \u2014 mechanically identical to host-thinness.sh. Contents: child-runtime/child-extension-runtime.ts, relocated from src/child-extension-runtime.ts, which measures 536 LOC under this counting. The move itself was byte-identical (sha256 87343410bfff4205fe711178e05a0f0a90630b5853f48fb0f6d4d2cef8578cf2); two path-reference lines were then updated in place (the type-only generated tool-schemas import and selfDigest's own-file URL), giving post-edit sha256 014a61b364e3d0e1ffe5e239e62847d363ebf5099843845802644ec3d74db7d0. sourceDelta is 0 because no logic, control flow, or enforcement line was edited and the LOC count is identical before and after. This is a NEW governance surface, not a raise: those 536 lines were previously counted against HOST_BUDGET, and the host ledger's paired entry surrenders the corresponding slack (1207 -> 1123 = -84, being -91 removal of the runtime's baseline lines from Host scope plus +7 retained legitimate growth in src/pi-coding-agent-shim.d.ts). The seed is deliberately flush against the actual so that parking a new file in child-runtime/, or growing this one, fails immediately and must be justified here rather than absorbed by discretionary headroom. HONEST DISCLOSURE: this module remains in the parent Host's import graph (extensions/autopilot.ts -> src/generated/child-extension.ts -> child-runtime/child-extension-runtime.ts) and is still loaded into the operator's Pi session; the relocation neither unloads it from the Host nor thins the Host. What the parent reaches is registerSubmitTools and CHILD_RECEIPT_ENTRY; runAutopilotChild and all delivery-policy enforcement are reachable only in a spawned child. Any later increase requires a new before/after measurement, itemized justification of each added confinement check, and architecture-owner approval."
  },
  {
    "from": 536,
    "to": 578,
    "actualBefore": 536,
    "actualAfter": 578,
    "sourceDelta": 42,
    "date": "2026-08-04",
    "justification": "UPWARD +42, itemized, for the mkdir/ENOENT confinement fix integrated from the content worker. WHY: createDeliveryWriteOperations.mkdir validated but NEVER CREATED, while the pinned framework @earendil-works/pi-coding-agent@0.83.0 write tool always calls ops.mkdir(dirname(absolutePath)) before writeFile (its default is fsMkdir(dir,{recursive:true})). Empirically reproduced: an approved unit file whose parent directory did not already exist failed with raw ENOENT, making any assignment that adds a new module directory undeliverable -- an unresumable LIVE failure mode. Every pre-existing test pre-created its directories, which is why nothing caught it. WHAT THE +42 BUYS (no line is decorative): ancestorDirectoryChain() enumerates the full approved ancestor chain from the assignment (authority derives from the assignment, never from the filesystem or the argument); createApprovedDirectoryChain() walks it top-down re-asserting EXACT membership at every level (no prefix widening); ensureApprovedDirectoryLevel() creates one level at a time with mkdirSync(dir) and NO recursive option so an unexpected EEXIST fails loudly, then re-lstats the created level and rejects a symlink or non-directory, closing the swap-a-symlink-between-components race; and assertMutationTopology's ancestor walk now tolerates a not-yet-existing directory ONLY after that directory has already passed approved-ancestor membership, so unapproved directories still raise their raw ENOENT. NOT A WIDENING OF FILE AUTHORITY: file writes still gate on policy.allowedAbsolutePaths (exact membership); the widened allowedParentDirectories set governs directory creation only. Verified by orchestrator. EVIDENCE: 8 new regression tests T1-T8 in host/tests/delivery-policy-tools.test.ts, driving the REAL framework tools. T1 deliberately does NOT pre-create the directory and was proven to FAIL before the fix and PASS after (T5/T6/T7 also discriminate), so this does not pin the broken behaviour (BUG-48 RC1 anti-pattern avoided). T2/T3 assert the rejected directory does not exist on disk afterwards, not merely that an error was thrown. T6 pins that enumerated-ancestor authority did not widen back into prefix authority. `recursive` appears nowhere in the file. This ceiling is again flush against the measured actual with zero discretionary headroom."
  }
]
