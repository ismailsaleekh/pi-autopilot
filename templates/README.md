# Autopilot templates

This directory contains package-owned fixed child role templates for Autopilot. The prompt renderer resolves this directory relative to the package source so installed users do not depend on project-local template files.
